# Gemini instructions

The source of truth is `AGENTS.md`, followed by `docs/00_PRODUCT_CONTRACT.md`, `docs/01_ARCHITECTURE.md`, and the private `do/state.md` note when present. Do not propose or implement a controller device, cloud mailbox, automatic clipboard watcher, or delayed direct clipboard overwrite.
