# Work Order 08 — post-v1 file transfer (currently blocked)

**Do not execute this work order until the owner records a final GO for text-only v1 and explicitly unfreezes file transfer.**

When unfrozen, read `docs/10_FILE_TRANSFER_FUTURE.md`, preserve symmetric direct peer operation, and create a separate protocol/storage lane for bounded file streaming. Do not alter clipboard-push semantics or turn any device into an offline mailbox.
