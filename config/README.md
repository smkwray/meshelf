# Configuration examples

These files contain placeholders only. Never commit real keys, fingerprints, Tailscale addresses, or private clipboard content.

The Tailscale grant is optional defense in depth and must be adapted to the owner's tailnet policy. It does not replace app-level pairing.
